import 'mobil.dart';

class Container {
  var nama;
  var kapasitas;
  List<Mobil> muatan = [];

  void tambahMuatan(Mobil muatanBaru) {
    // Hitung kapasitas tersedia

    // Bandingkan kapasitas tersedia dengan bobot dari muatan baru

    // Bila masih muat maka tambahkan ke dalam muatan

    // Bila tidak muat, tolak penambahan muatan
  }
}
